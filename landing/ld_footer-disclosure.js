(() => {
  const COPY = {
    es: {
      tagline: "Conectamos personas, m\u00e1quinas y procesos.",
      credits: "Tecnolog\u00eda y cr\u00e9ditos",
      technology:
        "Desarrollado con herramientas de OpenAI Codex. Autenticaci\u00f3n, datos y almacenamiento sobre Firebase de Google; c\u00f3digo y publicaci\u00f3n con GitHub.",
      contact: "Contacto",
      email: "Correo electr\u00f3nico",
      phone: "Tel\u00e9fono",
      hours: "De lunes a viernes, de 09:00 a 15:00 (Horario de Espa\u00f1a / CET).",
      address: "07440 Muro, Mallorca, Espa\u00f1a",
      hoursLabel: "Horario",
      addressLabel: "Direcci\u00f3n",
      legal: "UNATOMO CORE SL \u00b7 Todos los derechos reservados."
    },
    en: {
      tagline: "We connect people, machines, and processes.",
      credits: "Technology and credits",
      technology:
        "Developed using tools from OpenAI Codex. Authentication, data, and storage on Google Firebase; code and publishing with GitHub.",
      contact: "Contact",
      email: "Email",
      phone: "Phone",
      hours: "Monday to Friday, from 09:00 to 15:00 (Spain time / CET).",
      address: "07440 Muro, Mallorca, Spain",
      hoursLabel: "Opening hours",
      addressLabel: "Address",
      legal: "UNATOMO CORE SL \u00b7 All rights reserved."
    },
    it: {
      tagline: "Connettiamo persone, macchine e processi.",
      credits: "Tecnologia e crediti",
      technology:
        "Sviluppato con strumenti di OpenAI Codex. Autenticazione, dati e archiviazione su Google Firebase; codice e pubblicazione con GitHub.",
      contact: "Contatto",
      email: "Email",
      phone: "Telefono",
      hours: "Dal luned\u00ec al venerd\u00ec, dalle 09:00 alle 15:00 (Orario della Spagna / CET).",
      address: "07440 Muro, Maiorca, Spagna",
      hoursLabel: "Orari",
      addressLabel: "Indirizzo",
      legal: "UNATOMO CORE SL \u00b7 Tutti i diritti riservati."
    },
    el: {
      tagline: "\u03a3\u03c5\u03bd\u03b4\u03ad\u03bf\u03c5\u03bc\u03b5 \u03b1\u03bd\u03b8\u03c1\u03ce\u03c0\u03bf\u03c5\u03c2, \u03bc\u03b7\u03c7\u03b1\u03bd\u03ad\u03c2 \u03ba\u03b1\u03b9 \u03b4\u03b9\u03b1\u03b4\u03b9\u03ba\u03b1\u03c3\u03af\u03b5\u03c2.",
      credits: "\u03a4\u03b5\u03c7\u03bd\u03bf\u03bb\u03bf\u03b3\u03af\u03b1 \u03ba\u03b1\u03b9 \u03b9\u03b4\u03b9\u03cc\u03c4\u03b7\u03c4\u03b5\u03c2",
      technology:
        "\u0391\u03bd\u03b1\u03c0\u03c4\u03cd\u03c7\u03b8\u03b7\u03ba\u03b5 \u03bc\u03b5 \u03b5\u03c1\u03b3\u03b1\u03bb\u03b5\u03af\u03b1 OpenAI Codex. \u039f \u03ad\u03bb\u03b5\u03b3\u03c7\u03bf\u03c2 \u03c4\u03b1\u03c5\u03c4\u03cc\u03c4\u03b7\u03c4\u03b1\u03c2, \u03c4\u03b1 \u03b4\u03b5\u03b4\u03bf\u03bc\u03ad\u03bd\u03b1 \u03ba\u03b1\u03b9 \u03b7 \u03b7\u03bb\u03b5\u03ba\u03c4\u03c1\u03bf\u03bd\u03b9\u03ba\u03ae \u03b1\u03c0\u03bf\u03b8\u03ae\u03ba\u03b5\u03c5\u03c3\u03b7 \u03b1\u03be\u03b9\u03bf\u03c0\u03bf\u03b9\u03bf\u03cd\u03bd \u03c4\u03bf Google Firebase, \u03b5\u03bd\u03ce \u03bf \u03ba\u03ce\u03b4\u03b9\u03ba\u03b1\u03c2 \u03ba\u03b1\u03b9 \u03b7 \u03b4\u03b7\u03bc\u03bf\u03c3\u03af\u03b5\u03c5\u03c3\u03b7 \u03b3\u03af\u03bd\u03bf\u03bd\u03c4\u03b1\u03b9 \u03bc\u03b5 GitHub.",
      contact: "\u0395\u03c0\u03b9\u03ba\u03bf\u03b9\u03bd\u03c9\u03bd\u03af\u03b1",
      email: "Email",
      phone: "\u03a4\u03b7\u03bb\u03ad\u03c6\u03c9\u03bd\u03bf",
      hours: "\u0394\u03b5\u03c5\u03c4\u03ad\u03c1\u03b1 \u03ad\u03c9\u03c2 \u03a0\u03b1\u03c1\u03b1\u03c3\u03ba\u03b5\u03c5\u03ae, 09:00 \u03ad\u03c9\u03c2 15:00 (\u038f\u03c1\u03b1 \u0399\u03c3\u03c0\u03b1\u03bd\u03af\u03b1\u03c2 / CET).",
      address: "07440 Muro, \u039c\u03b1\u03b3\u03b9\u03cc\u03c1\u03ba\u03b1, \u0399\u03c3\u03c0\u03b1\u03bd\u03af\u03b1",
      hoursLabel: "\u03a9\u03c1\u03ac\u03c1\u03b9\u03bf",
      addressLabel: "\u0394\u03b9\u03b5\u03cd\u03b8\u03c5\u03bd\u03c3\u03b7",
      legal: "UNATOMO CORE SL \u00b7 \u039c\u03b5 \u03b5\u03c0\u03b9\u03c6\u03cd\u03bb\u03b1\u03be\u03b7 \u03c0\u03b1\u03bd\u03c4\u03cc\u03c2 \u03b4\u03b9\u03ba\u03b1\u03b9\u03ce\u03bc\u03b1\u03c4\u03bf\u03c2."
    }
  };

  const getCopy = () => COPY[document.documentElement.lang] || COPY.es;

  const getNormalizedPath = () => window.location.pathname.replace(/\/+$/, "") || "/";

  const usesRootFooterLogo = () => {
    const path = getNormalizedPath();
    return path === "/" || path === "/index.html" || path === "/landing" || path === "/landing/index.html";
  };

  const usesContactFooter = () => {
    const path = getNormalizedPath();
    return (
      path === "/" ||
      path === "/index.html" ||
      path === "/landing" ||
      path === "/landing/index.html" ||
      path.startsWith("/landing/nosotros") ||
      path.startsWith("/landing/contacto")
    );
  };

  window.renderLandingDisclosureFooter = ({ legalFooterText = "" } = {}) => {
    const legalFooter = document.getElementById("legal-footer");
    if (!legalFooter) return;
    const copy = getCopy();
    if (typeof legalFooter.__updateLandingDisclosure === "function") {
      legalFooter.__updateLandingDisclosure({ copy, legalFooterText });
      return;
    }

    const privacyFooter = legalFooter.nextElementSibling?.classList.contains("footer-link")
      ? legalFooter.nextElementSibling
      : null;
    const privacyLink = privacyFooter?.querySelector("a") || null;
    legalFooter.textContent = "";
    legalFooter.classList.add("footer-disclosure", "landing-footer-disclosure");
    const rootFooterLogo = usesRootFooterLogo();
    legalFooter.classList.toggle("has-root-brand-logo", rootFooterLogo);

    const toggle = document.createElement("button");
    toggle.type = "button";
    toggle.className = "footer-disclosure-toggle";
    toggle.setAttribute("aria-label", "Footer");
    toggle.setAttribute("aria-expanded", "false");
    toggle.setAttribute("aria-controls", "landing-footer-disclosure-panel");
    toggle.innerHTML = '<span class="footer-disclosure-icon" aria-hidden="true"></span>';

    const panel = document.createElement("div");
    panel.id = "landing-footer-disclosure-panel";
    panel.className = "footer-disclosure-panel";
    panel.setAttribute("role", "region");
    panel.setAttribute("aria-label", "Footer");
    panel.hidden = true;

    const identity = document.createElement("div");
    identity.className = "footer-disclosure-identity";
    const tagline = document.createElement("p");
    tagline.className = "footer-disclosure-tagline";
    const company = document.createElement("p");
    company.className = "footer-disclosure-company";
    if (rootFooterLogo) {
      company.classList.add("is-reserved-space");
      company.setAttribute("aria-hidden", "true");
    } else if (getNormalizedPath().startsWith("/landing/nosotros")) {
      company.append("Powered by ");
      const ourselvesLink = document.createElement("a");
      ourselvesLink.href = "#landing-topbar-mount";
      ourselvesLink.textContent = "ourselves";
      ourselvesLink.addEventListener("click", (event) => {
        event.preventDefault();
        window.scrollTo({ top: 0, behavior: "smooth" });
      });
      company.append(ourselvesLink, ".");
    } else {
      company.append("Powered by ");
      const poweredByLink = document.createElement("a");
      poweredByLink.href = "https://unatomo.com/";
      poweredByLink.textContent = "UNATOMO";
      company.appendChild(poweredByLink);
    }
    identity.appendChild(tagline);
    identity.appendChild(company);
    panel.appendChild(identity);

    const meta = document.createElement("div");
    meta.className = "footer-disclosure-meta";
    const creditsTitle = document.createElement("div");
    creditsTitle.className = "footer-disclosure-credits-title";
    meta.appendChild(creditsTitle);
    const contactFooter = usesContactFooter();
    let technologyNote = null;
    let contactHours = null;
    let contactAddress = null;
    let contactEmailIcon = null;
    let contactPhoneIcon = null;
    let contactHoursIcon = null;
    let contactAddressIcon = null;

    if (contactFooter) {
      const contactList = document.createElement("div");
      contactList.className = "footer-disclosure-contact-list";
      const createContactItem = ({ href = "", iconPath, text }) => {
        const item = document.createElement(href ? "a" : "div");
        item.className = "footer-disclosure-contact-item";
        if (href) item.href = href;
        const icon = document.createElement("span");
        icon.className = "footer-disclosure-contact-icon";
        icon.setAttribute("aria-hidden", "true");
        icon.innerHTML = `<svg viewBox="0 0 24 24"><path d="${iconPath}"></path></svg>`;
        const value = document.createElement("span");
        value.textContent = text;
        item.append(icon, value);
        contactList.appendChild(item);
        return { item, value };
      };
      const emailItem = createContactItem({
        href: "mailto:info@unatomo.com",
        iconPath: "M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2zm0 2v.01L12 13 20 6.01V6H4zm0 12h16V8l-8 7-8-7v10z",
        text: "info@unatomo.com"
      });
      const phoneItem = createContactItem({
        href: "tel:+34871252049",
        iconPath: "M6.62 10.79a15.05 15.05 0 006.59 6.59l2.2-2.2a1 1 0 011.01-.24 11.36 11.36 0 003.56.57 1 1 0 011 1V20a2 2 0 01-2 2A17 17 0 013 5a2 2 0 012-2h2.5a1 1 0 011 1 11.36 11.36 0 00.57 3.56 1 1 0 01-.24 1.01l-2.21 2.22z",
        text: "+34 871 25 20 49"
      });
      const hoursItem = createContactItem({
        iconPath: "M12 2a10 10 0 1010 10A10.011 10.011 0 0012 2zm0 18a8 8 0 118-8 8.009 8.009 0 01-8 8zm.5-13h-1v6l5 3 .5-.86-4.5-2.64z",
        text: ""
      });
      const addressItem = createContactItem({
        href: "https://www.google.com/maps/search/?api=1&query=07440%20Muro%2C%20Mallorca%2C%20Espa%C3%B1a",
        iconPath: "M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z",
        text: ""
      });
      addressItem.item.target = "_blank";
      addressItem.item.rel = "noopener";
      contactEmailIcon = emailItem.item;
      contactPhoneIcon = phoneItem.item;
      contactHoursIcon = hoursItem.item;
      contactAddressIcon = addressItem.item;
      contactHours = hoursItem.value;
      contactAddress = addressItem.value;
      meta.appendChild(contactList);
    } else {
      const technologyLinks = document.createElement("div");
      technologyLinks.className = "footer-disclosure-tech-links";
      [
        { href: "https://openai.com/", label: "OpenAI" },
        { href: "https://openai.com/codex/", label: "Codex" },
        { href: "https://firebase.google.com/", label: "Firebase by Google" },
        { href: "https://github.com/", label: "GitHub" }
      ].forEach(({ href, label }) => {
        const anchor = document.createElement("a");
        anchor.href = href;
        anchor.target = "_blank";
        anchor.rel = "noopener";
        anchor.textContent = label;
        technologyLinks.appendChild(anchor);
      });
      technologyNote = document.createElement("p");
      technologyNote.className = "footer-disclosure-tech-note";
      meta.appendChild(technologyLinks);
      meta.appendChild(technologyNote);
    }
    panel.appendChild(meta);

    if (rootFooterLogo) {
      const logoWrap = document.createElement("div");
      logoWrap.className = "footer-disclosure-root-logo";
      const logo = document.createElement("img");
      logo.src = "/static/img/logo-unatomo-v1.5.svg";
      logo.alt = "UNATOMO";
      logo.loading = "lazy";
      logoWrap.appendChild(logo);
      panel.appendChild(logoWrap);
    }

    const legal = document.createElement("p");
    legal.className = "footer-disclosure-legal";
    const legalTextNode = document.createTextNode("");
    legal.appendChild(legalTextNode);
    if (privacyLink) {
      privacyLink.classList.add("footer-disclosure-privacy");
      const privacyRow = document.createElement("p");
      privacyRow.className = "footer-disclosure-privacy-row";
      privacyRow.appendChild(privacyLink);
      identity.appendChild(privacyRow);
      privacyFooter.remove();
    }
    identity.appendChild(legal);

    const control = document.createElement("div");
    control.className = "footer-disclosure-control";
    control.appendChild(toggle);
    control.appendChild(panel);
    legalFooter.appendChild(control);

    legalFooter.__updateLandingDisclosure = ({ copy: nextCopy, legalFooterText: nextLegal }) => {
      tagline.textContent = nextCopy.tagline;
      creditsTitle.textContent = contactFooter ? nextCopy.contact : nextCopy.credits;
      if (technologyNote) technologyNote.textContent = nextCopy.technology;
      if (contactHours) contactHours.textContent = nextCopy.hours;
      if (contactAddress) contactAddress.textContent = nextCopy.address;
      if (contactEmailIcon) contactEmailIcon.setAttribute("aria-label", nextCopy.email);
      if (contactPhoneIcon) contactPhoneIcon.setAttribute("aria-label", nextCopy.phone);
      if (contactHoursIcon) contactHoursIcon.setAttribute("aria-label", nextCopy.hoursLabel);
      if (contactAddressIcon) contactAddressIcon.setAttribute("aria-label", nextCopy.addressLabel);
      legalTextNode.nodeValue = `\u00a9 ${new Date().getFullYear()} ${nextLegal || nextCopy.legal}`;
    };
    legalFooter.__updateLandingDisclosure({ copy, legalFooterText });

    let transitionTimer = null;
    let collapseTimer = null;
    let openFrame = null;
    let scrollFrame = null;
    const clearTransition = () => {
      if (transitionTimer !== null) window.clearTimeout(transitionTimer);
      if (collapseTimer !== null) window.clearTimeout(collapseTimer);
      if (openFrame !== null) window.cancelAnimationFrame(openFrame);
      if (scrollFrame !== null) window.cancelAnimationFrame(scrollFrame);
      transitionTimer = null;
      collapseTimer = null;
      openFrame = null;
      scrollFrame = null;
    };
    const followExpansion = (until) => {
      if (!control.classList.contains("is-open")) {
        scrollFrame = null;
        return;
      }
      const viewportBottom = window.innerHeight || document.documentElement.clientHeight;
      const overflow = control.getBoundingClientRect().bottom + 16 - viewportBottom;
      if (overflow > 0) window.scrollBy({ top: overflow, left: 0, behavior: "auto" });
      if (performance.now() < until) {
        scrollFrame = window.requestAnimationFrame(() => followExpansion(until));
      } else {
        scrollFrame = null;
      }
    };
    const closePanel = ({ restoreFocus = false } = {}) => {
      if (panel.hidden) return;
      clearTransition();
      control.classList.remove("is-opening");
      control.classList.add("is-closing");
      toggle.setAttribute("aria-expanded", "false");
      const reducedMotion = window.matchMedia?.("(prefers-reduced-motion: reduce)").matches;
      collapseTimer = window.setTimeout(() => {
        control.classList.remove("is-open");
        collapseTimer = null;
        transitionTimer = window.setTimeout(() => {
          panel.hidden = true;
          control.classList.remove("is-closing");
          transitionTimer = null;
        }, reducedMotion ? 0 : 340);
      }, reducedMotion ? 0 : 120);
      if (restoreFocus) toggle.focus();
    };
    toggle.addEventListener("click", (event) => {
      event.preventDefault();
      event.stopPropagation();
      const opening = toggle.getAttribute("aria-expanded") !== "true";
      if (!opening) {
        closePanel();
        return;
      }
      clearTransition();
      control.classList.remove("is-closing");
      control.classList.add("is-opening");
      panel.hidden = false;
      toggle.setAttribute("aria-expanded", "true");
      openFrame = window.requestAnimationFrame(() => {
        control.classList.remove("is-opening");
        control.classList.add("is-open");
        openFrame = null;
        const reducedMotion = window.matchMedia?.("(prefers-reduced-motion: reduce)").matches;
        followExpansion(performance.now() + (reducedMotion ? 0 : 380));
      });
    });
    document.addEventListener("click", (event) => {
      if (!legalFooter.contains(event.target)) closePanel();
    });
    document.addEventListener("keydown", (event) => {
      if (event.key === "Escape" && !panel.hidden) {
        event.preventDefault();
        closePanel({ restoreFocus: true });
      }
    });
  };
})();
